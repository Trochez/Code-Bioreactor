NIL_WORKING_AREA(waThreadDoorStatus, 16);
NIL_THREAD(ThreadDoorStatus, arg) {


  while(true) {
    // We should check that a door does not stay open !
    // There is a limit of 15s for example before buzzer turns out



    nilThdSleepMilliseconds(1);
  }
}





